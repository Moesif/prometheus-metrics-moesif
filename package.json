{
  "name": "prometheus-metrics-moesif",
  "version": "1.0.0",
  "description": "Prometheus metrics exporter that pulls data from Moesif Management API",
  "license": "Apache-2.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^4.21.0",
    "prom-client": "^15.1.0"
  },
  "devDependencies": {
    "jest": "^30.3.0"
  }
}
